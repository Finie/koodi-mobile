import React from 'react';
import {View, StyleSheet, Text} from 'react-native';
import {images} from '../../constants';

const Home = ({navigation}) => {
  return (
    <View>
      <Text>Welcome to Gladys</Text>
    </View>
  );
};
const styles = StyleSheet.create({});

export default Home;
